{
  "name": "folio-web",
  "version": "1.0.0",
  "private": true,
  "description": "Monaco Editor bundle for Folio's Tauri shell.",
  "scripts": {
    "build": "node copy-monaco.js && esbuild editor.ts --bundle --format=iife --target=es2018 --platform=browser --outfile=../dist/editor.bundle.js --minify --legal-comments=none"
  },
  "dependencies": {
    "monaco-editor": "^0.52.2"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
