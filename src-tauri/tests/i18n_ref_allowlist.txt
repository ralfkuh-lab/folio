# I18n reference-test allowlist (I1c).
# Format: one key prefix per line. Lines starting with # are comments.
# Each entry MUST carry a justification comment on the same line or the
# preceding line. Prefixes mark dynamically composed registry keys that the
# scanner cannot see as string literals; dead-key soft mode treats matching
# catalog keys as referenced.
#
# Allowlist entries that match catalog keys without a code string-literal hit
# are hard errors (I4a F2). Built-in theme/action keys live as string literals
# in i18n::THEME_BUILTIN_CATALOG / AI_ACTION_BUILTIN_CATALOG — no allowlist.
