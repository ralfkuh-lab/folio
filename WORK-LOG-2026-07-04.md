# Arbeits-Log 2026-07-04 (folio)

> **Temporäre Handover-Datei — nicht committen.** Dient nur der
> Session-Übergabe. Nach Abschluss löschen.

## Zweck

Session-Übergabe: Die vorherige Session (Claude Fable 5) wurde
zwischendurch auf Opus 4.8 herabgestuft; der User startet neu. Diese
Datei hält den vollständigen Arbeitsstand fest, der Übergabeprompt
steht in [`HANDOVER.md`](HANDOVER.md).

## Heute abgeschlossen & auf `main` gepusht

Chronologisch (alle Commits gepusht, Working Tree sauber, Stand
`3e5ed72`):

1. `e2bc939` **feat(split)**: Draggbarer Mid-Splitter im Split-Mode
   (`#splitter-mid`, CSS-Var `--split-mid`, Persistenz
   `split_mid_percent` in `panel_state.rs`/`panel-state.json`, Command
   `set_split_mid_percent`, Boot-Restore `split_mid_get`,
   Drag-Race-Schutz via `applySplitMidFromBackend`). E2E-Baseline
   `21_split_mode` neu erzeugt.
2. `8ecbfc7` **feat(linux)**: `.md`-Icon-Integration in deb+rpm
   (hicolor-Icons 16–256, MIME-XML, `install-folio-icons.sh` ins
   Paket), Linux-Hilfe-Menüpunkt „Markdown-Icon-Integration
   einrichten…" (`commands/app/icon_integration.rs`), Recommends
   `python3-pil`/`python3-pillow`.
3. `d0900af` **docs(todo)**: TODO aufgeräumt + neue User-Wünsche.
4. `754d0c8` **feat(e2e)**: Einzelszenario-Läufe
   (`run-e2e.sh 21_split_mode` / Präfix), Single-Instance-Vorabcheck
   im Wrapper (pgrep), `record_only` bei Teil-Läufen (kein
   Baseline-Vergleich).
5. `98c59b7` **docs(todo)**: E2E-Zustands-Leaks-Punkt +
   Single-Instance-Empfehlung dokumentiert.
6. `025d205` **feat(automation)**: Acks auf allen mutierenden
   Endpoints (`/theme`, `/rail`, `/workspace/pin|unpin`, `/history/*`,
   `/editor/text`) via optionalem `requestId` in geteilten Events;
   `ApiQuery`-Extractor (JSON-Fehlermodell für Query-Rejections);
   `GET/POST /settings` (reuse `settings_update`-Pfad inkl.
   Side-Effects); `POST /split`; `splitMidPercent` in `/state`;
   E2E-Szenario `23_api_settings_split`.
7. `a46b6cb` **feat(export)**: Export-Zielverzeichnis-Default
   (`pick_export_target` startet im Dokument-Verzeichnis oder zuletzt
   gewähltem Verzeichnis), Setting `exportDirMode` (`document`/`last`),
   `WorkspaceData.last_export_dir` (Forward-Slash-normalisiert).
8. `3e5ed72` **feat(settings)**: Settings-Dialog auf vertikales
   Tab-Control (Tabs „Allgemein" + „Diagnose", ARIA, ↑/↓-Nav, Reset
   auf „Allgemein" beim Öffnen). Neue stabile Selektoren
   `settings-tab-*`/`data-settings-tab`. E2E-Szenario
   `24_settings_tabs`. Struktur so gebaut, dass weitere Tabs nur
   Nav-Button + Panel brauchen.

E2E-Suite steht bei **24 Szenarien, zuletzt komplett grün**.

## Aktueller Arbeitsschritt: Settings-Ausbau (3 von 3 Punkten)

Der User hatte gebeten, drei Punkte selbständig nacheinander
abzuarbeiten:
1. ✅ Export-Zielverzeichnis-Default (`a46b6cb`).
2. ✅ Settings-Dialog auf Tab-Control (`3e5ed72`).
3. ⏳ **Markdown-Themes-Tab mit Theme-System** — HIER stehen wir,
   noch KEIN Code geschrieben.

### Punkt 3 ist in 4 Etappen geschnitten (je eigener Commit)

- **3a — Theme-Fundament + View-Theme-Auswahl (Light/Dark)** ← als
  Nächstes
- **3b — Custom-Themes (User-CSS)**
- **3c — Theme-Favoriten + Export-Priorisierung**
- **3d — Code-Highlighting im Export (syntect)**

### Design-Entscheidungen des Users (verbindlich, bereits geklärt)

- **Light/Dark**: Themes bekommen Light- UND Dark-Variante. Hat ein
  Theme keine Dark-Variante → Fallback auf WYSIWYG (Light-CSS auch im
  App-Dark-Mode). In der Theme-Auswahl **anzeigen**, ob ein Theme
  Light/Dark unterstützt.
- **Code-Highlighting im Export**: **gleich mit rein** (Etappe 3d),
  statischer Backend-Highlighter (syntect o. ä.), Light/Dark passend
  zum gewählten Theme. Die **View bleibt bei Monaco** fürs Highlighting
  (tief integriert, Umstellung wäre Regressions-Risiko) → Export und
  View werden *ähnlich*, nicht pixelgleich. Bewusster Kompromiss.
- Selbst entschieden (im Rahmen): Persistenz von aktivem View-Theme +
  Favoriten in `settings.json`; Custom-CSS unter
  `~/.config/folio/themes/`; heutiger View-Look bleibt als
  theme-aware „Standard"-Theme (Default) erhalten.

### Technische Kartierung für 3a (agy + eigene Exploration)

- **Export-Layouts**: `src-tauri/src/export.rs` — `LayoutInfo`
  (`id`/`name`/`description`), const `LAYOUTS` (classic/clean/github),
  CSS via `include_str!` aus `src-tauri/src/layouts/*.css` + `base.css`.
  Funktionen `layout_css(id)`, `render_document`, `wrap_html`.
- **Konflikt**: Layout-CSS stylt global `html, body` (z. B.
  `layouts/github.css:12`) → würde die In-Page-View zerlegen. **Muss
  auf `.markdown-body` gescopt werden.**
- **View-Rendering**: Ziel `#view-region main.markdown-body`
  (`src-tauri/web/app/view/markdown.ts:106` `getRoot()`), Styling
  statisch in `src-tauri/web/styles/content.css:167-264`, theme-aware
  über CSS-Variablen aus `base.css`, umgeschaltet via
  `html.theme-dark`/`.theme-light` (`editor/shell.ts`).
- **Code-Highlighting**: View nutzt Monaco `colorize`
  (`view/code-highlight.ts`), Export heute gar keins.
- **Settings-Dialog**: jetzt Tabs (`settings-tab-allgemein`,
  `settings-tab-diagnose`), Logik `settings-dialog.ts`
  (`activateSettingsTab`, `bindTabs`). Neuer Tab braucht nur
  Nav-Button + Panel (`data-settings-tab="<slug>"`, ID
  `settings-tab-<slug>`).
- **Export-Dialog**: `src-tauri/web/app/ui/export-dialog.ts` —
  Layout-Cards mit iframe-Preview via `export_render`.

## Offene TODO-Punkte nach Schritt 3 (siehe `TODO.md`)

Größere: Multi-Datei-Tabs (mit Single-Instance-Empfehlung), KI-Funktionen
(ai_config aus `~/dev/youtube-summarizer` als Crate herauslösbar,
Übersetzung als erstes Feature). Kleinere/Windows-gebunden:
Menu-Keybindings-Restpunkt, E2E-Windows-Baselines, E2E-Zustands-Leaks.

## Arbeitskonventionen dieser Session (wichtig!)

- **Codex CLI ist der bevorzugte Implementierungs- UND Review-Weg**
  (User hat Nutzungslimit-Resets, Modell gpt-5.5 high). Claude macht
  Oberaufsicht/QK + ist Fallback bei Codex-Limit. Siehe Memory
  `feedback_use_codex_reviews.md`.
  - Implementieren: `codex exec -s workspace-write -C /home/ralf/dev/folio "<task>"`
    (lange Specs als Datei in den scratchpad schreiben und im Prompt
    referenzieren — hat gut funktioniert).
  - Review: `codex exec "<review-prompt>"` (Achtung: `codex exec
    review --uncommitted` verträgt KEIN Prompt-Argument).
  - Codex-Limit-Reset: nur via `/usage` in der interaktiven TUI, kein
    CLI-Subcommand. Fallback ungetestet — im Zweifel User informieren
    und selbst weiterimplementieren.
- **agy** (Gemini) für breite Exploration/Recherche/Kartierung.
- **Immer verifizieren** (Codex-Output ist gut, aber prüfen): aus
  `src-tauri/`: `cargo test`, `cargo clippy --all-targets -- -D warnings`,
  `cargo fmt --check`; Frontend `cd src-tauri/web && npm run build &&
  npm test`. **Bundles in `src-tauri/dist/` sind eingecheckt und müssen
  bei TS-Änderungen neu gebaut werden.**
- **E2E**: `bash scripts/run-e2e.sh` (Linux+Xvfb). Bricht ab, wenn eine
  Folio-Desktop-Instanz des Users läuft (single-instance) → User bitten
  zu schließen. Einzelszenario: `bash scripts/run-e2e.sh <präfix>`
  (nur funktional, kein Visual-Vergleich). Wrapper baut das
  Release-Binary jetzt IMMER vor dem Lauf (sonst testet man stale Code).
  Neue Szenarien: **rein funktional, kein `ctx.screenshot`** (schont
  Visual-Baselines).
- **Screenshots** (nur wenn nötig): Fenster ~1/4, PNG vor `Read` auf
  50 % runterskalieren (Vision-Token sparen).
- **Commits**: direkt auf `main` (so vom User heute gewünscht),
  committen+pushen erst wenn der User es sagt bzw. pro fertiger Etappe
  nach grünem E2E. Message endet mit
  `Co-Authored-By: Claude <noreply@anthropic.com>` und Hinweis „mit
  Codex implementiert, Review/QA durch Claude".
- **Sprache**: Deutsch. Keine ASCII-Ersetzung von Umlauten.
- **Nach jeder Etappe**: erledigten TODO-Eintrag entfernen; falls die
  E2E-Suite bei Fehlern automatisch einen „E2E-Run …"-Eintrag unter
  „Hohe Priorität" in `TODO.md` schreibt, nach dem Fix entfernen.

## Empfohlener nächster Schritt

Etappe **3a** speccen und an Codex geben. Grober Umfang:
1. `LayoutInfo` um Light/Dark-Info erweitern (z. B. `has_dark: bool`);
   `layout_css(id, dark)`.
2. Layout-CSS auf `.markdown-body` scopen (kein globales `html/body`);
   Dark-Varianten `*.dark.css` für github/classic/clean anlegen.
3. Setting `view_theme` (Default `"standard"` = heutiges content.css).
4. View lädt gewähltes Theme dark-aware (App-Dark + Theme-hat-Dark →
   Dark-CSS, sonst Light-Fallback); Neu-Anwenden bei Theme- UND
   Theme-Mode-Wechsel; darf Monaco-Highlighting/Marker-Lanes/TOC NICHT
   brechen.
5. Settings-Tab „Markdown-Themes": Theme-Liste mit Name, Beschreibung,
   Light/Dark-Indikator, Auswahl; Persistenz über `settings_update`.
6. Neues funktionales E2E-Szenario (Theme setzen via `/settings`,
   `/state` bzw. `/dom` prüfen; kein Screenshot).
7. Export-Verhalten in 3a **unverändert** lassen (dark-Nutzung im
   Export kommt später/ist Teil 3d-Kontext).
