# Review-Auftrag: A/B-Vergleich zweier Implementierungen (Tab-Kontextmenü)

Zwei Branches implementieren UNABHÄNGIG dieselbe Spec
`docs/spec-tab-context-menu.md` (auf main vorhanden):

- Branch `test/tabctx-agy` — Kandidat A (die letzten 2 Commits;
  Hinweis: der zweite Commit enthält Orchestrator-Fixes am
  E2E-Szenario — zwei Defekte des Kandidaten, siehe Commit-Message;
  in deine Bewertung von A einrechnen).
- Branch `test/tabctx-grok` — Kandidat B (letzter Commit).

Beide: alle Gates grün, E2E-Szenario 50 grün (B auf Anhieb, A nach den
zwei genannten Fixes).

Auftrag: Vergleiche beide Diffs gegen main (`git diff main..test/tabctx-agy`
bzw. `git diff main..test/tabctx-grok`) und gegen die Spec. Bewerte je
Kandidat und DIREKT vergleichend:

1. **Spec-Treue** (alle Verhaltensdetails: Dirty-Serie mit Abbruch,
   Stack-Dedup/Cap/close_all-Ausnahme, tote Pfade beim Restore,
   Disabled-Zustände via recentlyClosedCount, virtuelle Tabs außen vor).
2. **Korrektheit/Robustheit** (Races, Locking, Event-Konsistenz,
   Edge-Cases; Konventionstreue: i18n leaf-safe, tracing-Targets,
   safeInvoke, tabs:changed-Semantik, Session-Persistenz unberührt).
3. **Codequalität** (Passung zu Bestandsmustern, Verständlichkeit,
   tote/unnötige Teile, CSS-Wiederverwendung).
4. **Testqualität** (Rust/vitest/E2E: prüfen die Tests echtes Verhalten
   oder nur Implementierungsdetails? Lücken?).

Format der Ausgabedatei: Kurzes Fazit zuerst (WELCHER Kandidat sollte
gemerged werden und warum, 3–5 Sätze), dann Befunde je Kandidat
(kritisch/sollte/nice-to-have, Datei:Zeile, konkret), dann ggf.
„aus dem Verlierer übernehmen"-Liste (einzelne bessere Details).
KEINE Codeänderungen. Wenn etwas nicht prüfbar war, explizit sagen.
