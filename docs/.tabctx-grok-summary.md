# Tab-Kontextmenü — Implementierungs-Summary

**Was:** Rechtsklick auf Dokument-Tabs: Schließen / Alle anderen / Tabs rechts / Zuletzt geschlossenen wiederherstellen. Backend Closed-Stack (Cap 10, Session-only, nicht bei close_all), Command `tab_restore_last`, Payload-Feld `recentlyClosedCount`.

**Dateien (Kern):** `tab_manager.rs`, `commands/tabs.rs`, `state.rs`, `lib.rs`, `automation/{handlers/tabs,router,middleware}.rs`, `web/app/ui/tab-context-menu.ts`, `web/app/state/tabs.ts`, `web/app/main.ts`, `dist/index.html`, `styles/overlays.css`, 9 Locale-Kataloge + `context/keys.json` + fr-Fixture, vitest `tests/ui/tab-context-menu.test.ts`, E2E `50_tab_context.py` + `lib/api.py::tab_restore_last`.

**Gates:** `cargo test` grün (717 lib + Integration/Smoke) · `clippy -D warnings` grün · `fmt --check` grün · `npm run build` grün · `vitest run` 454/454 grün. E2E bewusst nicht gelaufen (Orchestrator).

**Offen:** Baselines für Screenshot `tab_context_menu_open` anlegen; kein Shortcut (Ctrl+Shift+T wartet auf Accelerator-Baustelle).
