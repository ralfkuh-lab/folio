Aufgabe: Implementiere das Tab-Kontextmenü nach docs/spec-tab-context-menu.md — die Spec ist verbindlich und vollständig; lies sie zuerst, dann die relevanten Bestandsdateien (src-tauri/src/tab_manager.rs, src-tauri/src/commands/tabs.rs, src-tauri/src/automation/handlers/ (tabs-Endpunkte), src-tauri/web/app/state/tabs.ts, src-tauri/web/app/vault/context-menu.ts als UI-Muster, src-tauri/dist/index.html, CLAUDE.md-Abschnitte zu i18n und Multi-Datei-Tabs).

Wichtige Konventionen (aus CLAUDE.md, hart geprüft in cargo test):
- i18n: neue Keys in ALLEN 9 Katalogen unter src-tauri/locales/, alphabetisch sortiert, identische Key-Mengen; jeder Key braucht einen englischen Kontextsatz in locales/context/keys.json; Frontend nutzt t().
- Frontend-Bundles nach TS-Änderungen bauen: cd src-tauri/web && npm run build.
- Keine eprintln!/println!; tracing mit target "folio::*". safeInvoke-Muster für IPC-Fehler im Frontend.
- tabs:changed ist die Quelle der Tab-Leisten-Sicht; Payload-Erweiterung recentlyClosedCount additiv.

Arbeitsschritte:
1. Backend (tab_manager.rs + commands/tabs.rs + lib.rs-Registrierung + Automation-Endpunkt POST /tabs/restore_last) inkl. Rust-Tests nach Spec-Abschnitt "Tests".
2. Frontend (ui/tab-context-menu.ts, Markup #tab-ctx-menu, Verdrahtung in main.ts, Export der Close-Routine aus state/tabs.ts falls nötig) inkl. vitest-Tests.
3. i18n-Keys + Kontextsätze.
4. E2E-Szenario tests/e2e/scenarios/50_tab_context.py nach Spec (Muster: bestehende Szenarien 29/30; lib/api.py für die HTTP-Helfer; Fixtures unter tests/e2e/fixtures/ nutzen oder eigene Temp-Dateien mit Aufräumen). KEINEN E2E-Lauf starten, keine Baselines anfassen.
5. Gates ausführen und Ergebnisse notieren: aus src-tauri/: cargo test (voll), cargo clippy --all-targets -- -D warnings, cargo fmt --check; aus src-tauri/web/: npm run build und npx vitest run. ALLE müssen grün sein.
6. Kurz-Zusammenfassung (max 10 Zeilen: was, welche Dateien, Gate-Ergebnisse, offene Fragen) nach docs/.tabctx-grok-summary.md.

Nicht committen. Halte die TUI-Ausgabe knapp.
